import { useEffect, useMemo, useState } from "react";
import type { ReactNode } from "react";
import { ArrowUpRight, Check, ChevronDown, Github, Linkedin, Mail, Play, RotateCcw, Send, Terminal } from "lucide-react";
import DecryptedText from "../components/DecryptedText";
import Galaxy from "../components/Galaxy";
import MagicBento from "@/components/MagicBento";
import PillNav from "@/components/PillNav";
import CaaoExplainer from "@/components/CaaoExplainer";
import { AccordionGallery, NavigationWheel, OptionWheel } from "@/components/PortfolioInteractions";
import logoLockup from "../content/logo-lockup.svg";
import resumePdf from "../content/Aditya_Singh_Resume_2026.pdf";

const signalHero = "/manus-storage/aditya-signal-hero_6bbc2892.jpg";
const signalMark = "/manus-storage/aditya-signal-mark_b5f58e35.png";
const texture = "/manus-storage/aditya-signal-texture_a607e15d.jpg";

const nav = [{ label: "about", path: "/about" }, { label: "experience", path: "/experience" }, { label: "research", path: "/research" }, { label: "projects", path: "/projects" }, { label: "skills", path: "/skills" }, { label: "interests", path: "/interests" }, { label: "contact", path: "/contact" }];
const focusItems = ["ML systems", "Model compression", "GPU memory", "Data platforms", "Product thinking"];

const projects = [
  { id: "edgevit", number: "01", title: "edgevit-compress", kicker: "Vision Transformer compression for edge inference", summary: "A four-stage DeiT pipeline that trades a controlled accuracy loss for a model small enough to deploy at the edge.", tags: ["PyTorch", "DeiT", "INT8"], result: "4.40 MB", detail: "Problem: Vision Transformers are accurate but too large and slow for edge deployment. The numbered pipeline runs fine-tune_teacher, prune_student, distill, and quantize stages in a CUDA-enabled torchGPU conda environment under WSL. Pruning alone takes accuracy from 80.01% to 66.07% while moving from 5.54M to 3.89M parameters and 21.20MB to 14.89MB. Distillation recovers most of the loss at the same pruned size: 66.07% → 74.27%. INT8 quantization then reduces 14.89MB to 4.40MB for 73.53% accuracy. The final model is roughly 4.8x smaller than the 80.01% FP32 baseline at a ~6.5 point accuracy cost. The pipeline is deliberately a compression tradeoff, not a claim that every metric improves.", architecture: "A fine-tuned DeiT-Small teacher supervises a pruned DeiT-Tiny student; the distilled student is then quantized to INT8. The sequence is intentionally staged so each intervention can be measured separately.", demonstrates: "This project demonstrates disciplined ML systems thinking: isolate the effect of pruning, recover quality with distillation, then quantify what quantization costs." },
  { id: "dbms", number: "02", title: "LLM Powered DBMS", kicker: "Natural language in. Optimized SQL out.", summary: "A three-service database interface that translates plain language into SQL without embedding model serving inside the API process.", tags: ["FastAPI", "SQLite", "LoRA"], result: "89.6% SQL accuracy", detail: "Problem: non-technical users need to query and manage relational data without knowing SQL. Flask + Jinja2 serves the frontend on port 5000, FastAPI exposes collections, columns, and query endpoints on port 8000, and a separate local LLM server runs on port 8080. The NL→SQL model is LoRA fine-tuned with r=8, lora_alpha=32, q_proj/v_proj targets, 8-bit NF4 double quantization, bfloat16 compute, gradient checkpointing, four-step accumulation, 200 steps, learning rate 1.5e-4, and Adafactor. A few-shot template covers CREATE, INSERT, and SELECT; post-processing extracts the first recognized SQL keyword line. SQLAlchemy reflection and JSON Schema validation keep CRUD generic across LLM-created tables. Results: 89.6% correct translation across 500 commands and a 21% onboarding-time reduction. The README honestly labels it a prototype, not production-ready.", architecture: "The separate inference service isolates GPU-heavy model serving from the lightweight API. Reflection replaces hardcoded ORM models, while a schema manager persists a JSON Schema for each natural-language-created table.", demonstrates: "This project demonstrates full-stack execution, GPU-aware service boundaries, fine-tuning control, and the discipline to document where a prototype’s claims stop." },
  { id: "resume", number: "03", title: "ResumeMatch", kicker: "A semantic gap analysis tool for job search", summary: "Embeddings, ATS signals, and actionable gap analysis in one focused productivity tool.", tags: ["Embeddings", "NLP", "Product"], result: "1–2 week scope", detail: "Problem: job seekers need more than a single opaque resume score. ResumeMatch uses sentence embeddings for semantic resume-to-job-description similarity, then adds gap analysis for missing requirements and ATS-style keyword checks. Semantic similarity catches related language that exact keyword overlap misses; the complementary checks make the result actionable. It was intentionally scoped as a deployable 1–2 week portfolio build for free-tier infrastructure, separate from the heavier model-systems work.", architecture: "The core score comes from sentence-level embeddings, while the gap and ATS layers translate model output into concrete edits a job seeker can make.", demonstrates: "This project demonstrates product judgment: choose an interpretable scope, combine a strong ML signal with practical heuristics, and ship a useful full-stack tool." },
  { id: "logs", number: "04", title: "Log / Clickstream Analytics", kicker: "A language model for web traffic", summary: "A from-scratch transformer, anomaly detector, and distributed data pipeline for synthetic and real access-log traffic.", tags: ["Kafka", "Hadoop", "PySpark"], result: "Synthetic traffic", detail: "Problem: simulate and analyze web traffic at scale, including irregular events. The system uses the Kaggle eliasdabbas/web-server-access-logs dataset from the zanbil.ir e-commerce site, plus a reverse-DNS lookup table. A transformer built from scratch learns the dataset distribution and generates synthetic traffic; a separate anomaly-detection model flags irregular events. Kafka handles streaming ingestion, Hadoop provides storage, and PySpark performs distributed batch and aggregate analysis across traffic patterns, error rates, and sessions. The low-level design was completed before implementation.", architecture: "Generation and anomaly detection remain separate concerns feeding a shared streaming and batch-analysis layer. Building the transformer from scratch exposes the modeling assumptions rather than hiding them behind an off-the-shelf generator.", demonstrates: "This project demonstrates from-scratch model building and systems decomposition across storage, streaming, distributed processing, and analytical workloads." }
];

const stages = [
  ["01", "Fine-tune teacher", "DeiT-Small becomes the high-accuracy reference model.", "80.01%", "21.20 MB", "5.54M params"],
  ["02", "Prune student", "DeiT-Tiny is reduced from 5.54M to 3.89M parameters.", "66.07%", "14.89 MB", "3.89M params"],
  ["03", "Distill", "Teacher logits recover signal lost during pruning.", "74.27%", "14.89 MB", "3.89M params"],
  ["04", "Quantize", "INT8 weights make the student edge-ready.", "73.53%", "4.40 MB", "3.89M params"]
];

function SectionHeading({ index, eyebrow, title, children }: { index: string; eyebrow: string; title: string; children?: ReactNode }) {
  return <div className="section-heading"><div className="section-index"><span>{index}</span><i /></div><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{children}</div></div>;
}

function Metric({ value, label }: { value: string; label: string }) { return <div className="metric"><strong>{value}</strong><span>{label}</span></div>; }

export default function Home({ section = "all" }: { section?: string }) {
  const visible = (name: string) => section === "all" || section === name;
  const pageLabel = section === "home" ? "index" : section;
  useEffect(() => { document.title = section === "home" ? "Aditya Singh — ML / Software Engineer" : `Aditya Singh — ${pageLabel}`; window.scrollTo(0, 0); }, [section, pageLabel]);
  const [openProject, setOpenProject] = useState("edgevit");
  const [pipelineRunning, setPipelineRunning] = useState(false);
  const [completed, setCompleted] = useState(0);
  const [activeStage, setActiveStage] = useState(0);
  const [showThesis, setShowThesis] = useState(false);
  const selectedStage = useMemo(() => stages[Math.min(activeStage, stages.length - 1)], [activeStage]);

  useEffect(() => {
    if (!pipelineRunning) return;
    if (completed >= stages.length) return;
    const timer = window.setTimeout(() => { setCompleted((current) => current + 1); setActiveStage((current) => Math.min(current + 1, stages.length - 1)); }, 900);
    return () => window.clearTimeout(timer);
  }, [completed, pipelineRunning]);

  const runPipeline = () => { setCompleted(0); setActiveStage(0); setPipelineRunning(true); };
  const resetPipeline = () => { setPipelineRunning(false); setCompleted(0); setActiveStage(0); };

  return <div className="site-shell" style={{ backgroundImage: `url(${texture})` }}>
    <header className="topbar"><PillNav items={nav} activeHref={section === "home" ? "/" : `/${section}`} /><a className="top-contact" href="/contact">Let’s talk <ArrowUpRight size={15} /></a></header>

    <main id="top">
      {visible("home") && <section className="hero hero-home"><Galaxy className="hero-galaxy" density={0.72} hueShift={176} speed={0.58} glowIntensity={0.34} saturation={0} mouseInteraction mouseRepulsion repulsionStrength={1.05} /><div className="hero-overlay" />
        <div className="hero-grid"><div className="hero-copy"><p className="hero-name">Aditya Singh</p><p className="eyebrow lime">Machine learning / software engineering / systems</p><h1><DecryptedText text="I build systems" animateOn="view" revealDirection="start" speed={88} delay={260} /><br /><span><DecryptedText text="that make the" animateOn="view" revealDirection="start" speed={88} delay={820} /></span><br /><em><DecryptedText text="constraint visible." animateOn="view" revealDirection="start" speed={88} delay={1380} /></em></h1><p className="hero-intro">M.Tech IT / ML at IIIT Allahabad. Former SDE intern at Amazon. I work at the seam between models, memory, and the products that carry them into the world.</p><div className="hero-actions"><a className="button button-primary" href="/projects#edgevit">Run the recorded pipeline <Play size={15} fill="currentColor" /></a><a className="button button-ghost" href="/projects">Inspect the work <ArrowUpRight size={15} /></a></div></div><div className="hero-aside"><div className="hero-aside-top"><span>Based in India</span><span>Available 2026</span></div><div className="hero-wheel-wrap"><p>Navigate the system</p><NavigationWheel /></div><div className="hero-aside-bottom"><span>01 / 08</span><span>Scroll to inspect</span></div></div></div>
      </section>}

      <div className={`content-frame ${section === "home" ? "content-frame-home" : ""}`}>{section !== "home" && <aside className="side-index"><div className="side-index-title">On this page</div>{nav.map((item, index) => <a key={item.label} href={item.path} className={section === item.label ? "is-active" : ""} aria-current={section === item.label ? "page" : undefined}><span>0{index + 1}</span>{item.label}</a>)}<div className="side-index-line" /></aside>}
      <div className="content-column">
        {visible("about") && <section id="about" className="section about-section"><SectionHeading index="01" eyebrow="Context / orientation" title="A systems-minded engineer." /><div className="about-grid"><div className="about-lede"><p className="lead">The useful question is rarely “does the model work?” It is usually “what does it cost to make it work here?”</p><p>That question has taken me from low-level GPU memory experiments to full-stack database interfaces. I like the middle layer: the part where an idea becomes a measurable system, where an elegant paper meets an imperfect device, and where a tradeoff has to be stated plainly.</p><p>Currently pursuing an M.Tech in Information Technology (Machine Learning) at IIIT Allahabad with a 9 CGPA. Previously, a B.Tech in Computer Science from RGPV Bhopal.</p></div><div className="about-facts"><div><span>2024—26</span><b>M.Tech IT / ML</b><small>IIIT Allahabad · 9 CGPA</small></div><div><span>2020—24</span><b>B.Tech Computer Science</b><small>RGPV Bhopal</small></div><div><span>01—06 / 2026</span><b>SDE Intern</b><small>Amazon</small></div><div><span>07—08 / 2023</span><b>ML Intern</b><small>Suvidha Foundation</small></div></div></div></section>}

        {visible("experience") && <section id="experience" className="section experience-section"><SectionHeading index="02" eyebrow="Experience / what shipped" title="Work, with the context intact." /><div className="experience-list"><article className="experience-row"><div className="experience-meta"><span>01</span><b>Amazon</b><small>Jan—Jun 2026</small></div><div className="experience-body"><div className="role-line"><h3>Software Development Engineer Intern</h3><span className="status-tag">recent</span></div><p className="experience-context"><b>Context.</b> Worked inside a production engineering environment where reliability, maintainability, and the ability to reason about the system mattered as much as the feature itself.</p><p><b>Contribution.</b> Built and iterated on software with an emphasis on clear interfaces, debuggable behavior, and the handoff from implementation to operation. The internship sharpened how I scope work: start from the failure mode, trace the data path, then make the smallest change that improves the whole system.</p><div className="inline-note"><Terminal size={14} /> Production systems reward the engineer who can see the edges.</div></div></article><article className="experience-row"><div className="experience-meta"><span>02</span><b>Suvidha Foundation</b><small>Jul—Aug 2023</small></div><div className="experience-body"><div className="role-line"><h3>Machine Learning Intern</h3><span className="status-tag">field work</span></div><p className="experience-context"><b>Context.</b> Applied ML in a setting where the output needed to be understandable and useful, not just technically plausible.</p><p><b>Contribution.</b> Worked through the complete iteration loop — preparing data, training models, validating behavior, and translating findings into decisions that a wider team could act on. It was an early lesson in building for the person on the other side of the prediction.</p></div></article></div></section>}

        {visible("research") && <section id="research" className="section research-section"><SectionHeading index="03" eyebrow="Thesis / pending patent" title="Virtual memory, rethought for training." /><CaaoExplainer /><div className="research-hero"><div><p className="eyebrow lime">M.Tech thesis · supervised by Prof. Satish Kumar Singh</p><h3>Implementing Virtual Memory Management in Pipelined CAAO: A High-Throughput VRAM-Efficient Training Approach</h3><p>CAAO — Component-Aware Asynchronous Offloading — maps the OS concepts of paging, swap, and eviction onto GPU/CPU training. The goal is not to pretend memory is infinite. It is to keep the next useful component moving while the current one computes.</p></div><div className="research-badge"><span>pending</span><b>patent</b><small>same work, different audience</small></div></div><div className="pipeline-stages"><div className="pipeline-stage"><span>PIPE 1</span><b>Prefetch</b><small>Async load C[i+1] from CPU → GPU VRAM</small></div><div className="pipeline-stage active"><span>PIPE 2</span><b>Compute</b><small>Forward / backward pass on active C[i]</small></div><div className="pipeline-stage"><span>PIPE 3</span><b>Offload</b><small>Async move inactive C[i−1] back to CPU</small></div><div className="pipeline-stage"><span>PIPE 4</span><b>Update</b><small>CPU performs optimizer updates in background</small></div></div><div className="research-deep-dive"><div className="research-block"><p className="eyebrow lime">01 / The VRAM wall</p><h3>Offloading is only useful if the GPU does not have to wait.</h3><p>Modern deep networks — especially Transformers and GANs — can outgrow the VRAM available on a single GPU. The standard response is to move inactive parameters to CPU RAM, but a synchronous stop-and-copy strategy halts computation while a large transfer crosses the PCIe bus. That creates a latency-throughput gap: the GPU is idle precisely when the system is trying to save memory.</p></div><div className="research-block"><p className="eyebrow lime">02 / Research objectives</p><div className="research-objectives"><div><b>01</b><span>Map classical OS virtual-memory constructs directly onto deep-learning execution graphs.</span></div><div><b>02</b><span>Orchestrate model components using a multi-stage instruction pipeline.</span></div><div><b>03</b><span>Monitor execution stages independently with asynchronous CUDA streams.</span></div><div><b>04</b><span>Eliminate PCIe transfer bottlenecks with DMA and pinned-memory architectures.</span></div></div></div><div className="research-block"><p className="eyebrow lime">03 / Positioning against prior art</p><div className="prior-art"><div><b>MemFerry · IEEE INFOCOM 2025</b><span>Selects layers for Direct Hardware Access computation, but relies on hybrid computation splits rather than a continuous sliding window.</span></div><div><b>ZenFlow · 2025</b><span>Uses an importance-aware, decoupled update pipeline, but its offloading policies do not adapt to layer-wise computational graphs.</span></div><div><b>ZeRO · SC20</b><span>Partitions model states across data-parallel GPUs, requiring multi-GPU clusters that exclude single-node commodity hardware.</span></div></div><p className="research-callout">CAAO targets a single commodity GPU with a deterministic sliding-window pipeline. Its differentiator is the direct mapping of paging, swap, and eviction semantics onto the execution graph rather than an ad hoc offloading heuristic.</p></div><div className="research-block"><p className="eyebrow lime">04 / Virtual memory mapping</p><div className="mapping-grid"><div><b>Physical pages</b><span>VRAM acts as the physical page frame; only the active working set is resident.</span></div><div><b>Swap space</b><span>CPU RAM backs inactive components, optimizer states, and dormant activations.</span></div><div><b>Demand loading</b><span>Prefetch pages components asynchronously before computation needs them.</span></div><div><b>Deterministic eviction</b><span>Backpropagation order decides what pages out and when, making the schedule predictable and safe.</span></div></div></div><div className="research-block"><p className="eyebrow lime">05 / The overlap condition</p><div className="equation-card"><code>T_compute ≥ max(T_page_in, T_page_out)</code><span>When compute takes at least as long as the slower transfer, both data movements are hidden behind execution and the pipeline avoids PCIe stalls.</span></div><div className="equation-grid"><div><span>Standard training VRAM</span><code>V_standard = Σ(|θᵢ| + |φᵢ| + |aᵢ|) + V_act</code><small>Every component’s parameters, gradients, and activations are held simultaneously.</small></div><div><span>CAAO peak VRAM</span><code>V_peak ≈ maxᵢ(|Page_outᵢ₋₁| + |Page_execᵢ| + |Page_inᵢ₊₁|) + V_act</code><small>Peak memory is bounded by the largest three-component sliding-window overlap.</small></div></div></div><div className="research-block"><p className="eyebrow lime">06 / Implementation detail</p><h3>Pinned memory is not an optimization flourish. It is the condition that makes asynchronicity real.</h3><p>Pageable CPU RAM can force the operating system to use bounce buffers, adding unpredictable delay and breaking the transfer overlap assumed by the inequality above. CAAO initializes the CPU backing store in page-locked memory so the DMA engine can move tensors directly.</p><div className="pinned-list"><span><b>DataLoader</b> inputs use <code>pin_memory=True</code>.</span><span><b>CPU-side parameters</b> are explicitly mapped to pinned memory.</span><span><b>Dormant activations</b> are chained with <code>.pin_memory()</code> for asynchronous retrieval.</span></div></div><div className="research-block"><p className="eyebrow lime">07 / Results with the tradeoff intact</p><div className="table-wrap research-table"><table><thead><tr><th>ViT method</th><th>Throughput (samples/s)</th><th>Peak Reserved VRAM (MB)</th></tr></thead><tbody><tr><td>Baseline</td><td>676.57</td><td>4910</td></tr><tr><td>Activation Checkpointing</td><td>484.19</td><td>2430</td></tr><tr><td>ZeRO-Offload</td><td>633.00</td><td>4766</td></tr><tr className="highlight-row"><td>VM Pinned Pipelined CAAO</td><td>361.04</td><td>1864</td></tr></tbody></table></div><div className="table-wrap research-table"><table><thead><tr><th>Autoencoder method</th><th>Throughput (samples/s)</th><th>Peak Reserved VRAM (MB)</th></tr></thead><tbody><tr><td>Baseline</td><td>11935.92</td><td>554</td></tr><tr><td>Activation Checkpointing</td><td>12350.17</td><td>522</td></tr><tr><td>ZeRO-Offload</td><td>15411.86</td><td>552</td></tr><tr className="highlight-row"><td>VM Pinned Pipelined CAAO</td><td>8310.63</td><td>422</td></tr></tbody></table></div><p className="research-callout">On ViT, CAAO cuts peak VRAM by ~62% — 4910 MB to 1864 MB — but throughput falls from 676.57 to 361.04 samples/s. On the Autoencoder, it again reaches the lowest VRAM at 422 MB, but also the lowest throughput at 8310.63 samples/s; ZeRO-Offload beats the baseline on both metrics in that comparison. The claim is narrower and more useful: on single commodity GPUs where the alternative is an out-of-memory crash, trading speed for the ability to run is a deliberate, disclosed tradeoff.</p></div><div className="research-block"><p className="eyebrow lime">08 / Scope, status, next steps</p><div className="future-grid"><div><b>Dynamic component sizing</b><span>Fuse multiple blocks adaptively using real-time PCIe bandwidth monitoring.</span></div><div><b>Multi-GPU virtual memory</b><span>Extend the framework across distributed nodes to pool VRAM across commodity GPUs.</span></div></div><p className="patent-note"><strong>Patent status:</strong> filed as a pending patent, separate from but based on the same underlying research submitted as Aditya’s M.Tech thesis at IIIT Allahabad, approved July 2026. “Pending” means filed and under review; it does not mean granted.</p></div></div><button className="text-toggle" onClick={() => setShowThesis(!showThesis)}>{showThesis ? "Collapse full thesis notes" : "Open full thesis notes"}<ChevronDown size={16} className={showThesis ? "rotate" : ""} /></button>{showThesis && <div className="thesis-notes"><p>The pipeline is positioned against MemFerry (IEEE INFOCOM 2025), ZenFlow (2025), and ZeRO (SC20), but frames the problem through virtual-memory semantics: components are treated as pages with explicit residency and movement. The result is a large VRAM saving at a real throughput cost. That tradeoff is the point, not a footnote.</p><div className="table-wrap"><table><thead><tr><th>ViT comparison</th><th>Throughput</th><th>Peak VRAM</th></tr></thead><tbody><tr><td>Baseline</td><td>676.57 samples/sec</td><td>4910 MB</td></tr><tr><td>Activation Checkpointing</td><td>484.19 samples/sec</td><td>2430 MB</td></tr><tr><td>ZeRO-Offload</td><td>633.00 samples/sec</td><td>4766 MB</td></tr><tr className="highlight-row"><td>VM Pinned Pipelined CAAO</td><td>361.04 samples/sec</td><td>1864 MB</td></tr></tbody></table></div><div className="metric-strip"><Metric value="−62%" label="ViT peak VRAM" /><Metric value="8310.63" label="Autoencoder samples/sec" /><Metric value="422 MB" label="Autoencoder peak VRAM" /></div></div>}</section>}

        {visible("projects") && <section id="projects" className="section projects-section"><SectionHeading index="04" eyebrow="Selected builds / applied systems" title="Projects that leave a trace." /><p className="projects-intro">Hover to inspect the signal. Click a card to open the full case study.</p><MagicBento items={projects} onSelect={(id) => setOpenProject(openProject === id ? "" : id)} /><div className="project-list project-details-list">{projects.filter((project) => openProject === project.id).map((project) => <article key={project.id} className="project-row open"><div className="project-summary project-summary-static"><div className="project-number">{project.number}</div><div className="project-title"><p className="eyebrow">{project.kicker}</p><h3>{project.title}</h3></div><div className="project-result"><span>result</span><b>{project.result}</b></div></div><div className="project-detail"><p>{project.detail}</p>{project.architecture && <div className="project-technical-grid"><div><span>Architecture / design</span><p>{project.architecture}</p></div><div><span>What it demonstrates</span><p>{project.demonstrates}</p></div></div>}{project.id === "edgevit" && <div className="table-wrap"><table><thead><tr><th>Variant</th><th>Accuracy</th><th>Model size</th><th>Params</th></tr></thead><tbody><tr><td>baseline_fp32</td><td>80.01%</td><td>21.20 MB</td><td>5.54M</td></tr><tr><td>pruned_fp32</td><td>66.07%</td><td>14.89 MB</td><td>3.89M</td></tr><tr><td>distilled_pruned_fp32</td><td>74.27%</td><td>14.89 MB</td><td>3.89M</td></tr><tr><td>baseline_int8</td><td>78.77%</td><td>5.99 MB</td><td>5.54M</td></tr><tr><td>pruned_int8</td><td>64.74%</td><td>4.40 MB</td><td>3.89M</td></tr><tr className="highlight-row"><td>distilled_pruned_int8</td><td>73.53%</td><td>4.40 MB</td><td>3.89M</td></tr></tbody></table></div>}{project.id === "edgevit" && <div className="edgevit-pipeline"><div className="pipeline-intro"><div><p className="eyebrow lime">Recorded pipeline / edgevit-compress</p><p>Run the four numbered stages as a transparent replay. This is the recorded sequence, not a live model job.</p></div><button className="button button-primary" onClick={pipelineRunning ? resetPipeline : runPipeline}>{pipelineRunning ? <><RotateCcw size={15} /> Reset run</> : <><Play size={15} fill="currentColor" /> Run pipeline</>}</button></div><div className="pipeline-console"><div className="console-bar"><span><i /> edgevit-compress / replay.log</span><span>{pipelineRunning ? `${completed}/${stages.length} stages` : "idle"}</span></div><div className="stage-trace">{stages.map((stage, index) => <div key={stage[0]} className={`trace-stage ${index < completed ? "done" : ""} ${index === activeStage && pipelineRunning ? "current" : ""}`}><div className="trace-node">{index < completed ? <Check size={14} /> : stage[0]}</div><div><b>{stage[1]}</b><span>{stage[2]}</span></div></div>)}</div><div className="console-output"><div className="console-line"><span>› status</span><b>{pipelineRunning ? completed === stages.length ? "run complete" : "executing" : "ready"}</b></div><div className="console-line"><span>› active record</span><b>{pipelineRunning || completed ? selectedStage[1] : "—"}</b></div><div className="console-metrics"><Metric value={pipelineRunning || completed ? selectedStage[3] : "—"} label="accuracy" /><Metric value={pipelineRunning || completed ? selectedStage[4] : "—"} label="model size" /><Metric value={pipelineRunning || completed ? selectedStage[5] : "—"} label="parameter count" /></div></div></div></div>}</div></article>)}</div></section>}


        {visible("skills") && <section className="section skills-section"><SectionHeading index="05" eyebrow="Tools / working vocabulary" title="The stack is a means, not a personality." /><div className="skills-layout"><div className="skills-cloud"><span className="skill-major">PyTorch</span><span>Python</span><span className="skill-major">Transformers</span><span>FastAPI</span><span>SQL</span><span>Kafka</span><span className="skill-major">Deep Learning</span><span>Docker</span><span>PySpark</span><span>Hadoop</span><span>Git</span><span>Linux</span><span>Computer Vision</span><span>Embeddings</span><span>JavaScript</span></div><OptionWheel items={focusItems} /></div></section>}

        {visible("interests") && <section className="section gallery-section"><div className="gallery-copy"><p className="eyebrow lime">A little outside the benchmark</p><h2>Curiosity is part of the system.</h2><p>Professional work stays in the foreground. The rest — games, films, cars — is a quieter set of inputs.</p></div><AccordionGallery items={[{ image: "https://images.unsplash.com/photo-1511512578047-dfb367046420?auto=format&fit=crop&w=900&q=80", label: "Games", meta: "timing / worlds" }, { image: "https://images.unsplash.com/photo-1485846234645-a62644f84728?auto=format&fit=crop&w=900&q=80", label: "Films", meta: "frames / stories" }, { image: "https://images.unsplash.com/photo-1503376780353-7e6692767b70?auto=format&fit=crop&w=900&q=80", label: "Cars", meta: "motion / form" }, { image: "https://images.unsplash.com/photo-1531297484001-80022131f5a1?auto=format&fit=crop&w=900&q=80", label: "Systems", meta: "always learning" }]}/></section>}

        {visible("contact") && <section id="contact" className="section contact-section"><div className="contact-card"><p className="eyebrow lime">Open channel / 2026</p><h2>Let’s make the<br /><em>constraint useful.</em></h2><p>If you are building ML products, memory-constrained systems, or tools that make technical work easier to inspect, I would like to hear what you are working on.</p><div className="contact-links"><a href="mailto:singh3112aditya@gmail.com"><Mail size={16} /> singh3112aditya@gmail.com <ArrowUpRight size={14} /></a><a href="https://github.com/Aditya-Singh-3112" target="_blank" rel="noreferrer"><Github size={16} /> GitHub <ArrowUpRight size={14} /></a><a href="https://www.linkedin.com/in/aditya-singh-2a2066212/" target="_blank" rel="noreferrer"><Linkedin size={16} /> LinkedIn <ArrowUpRight size={14} /></a><a href={resumePdf} download="Aditya_Singh_Resume_2026.pdf"><Send size={16} /> Resume / PDF <ArrowUpRight size={14} /></a></div></div></section>}
      </div></div>
    </main>
    <footer><span>© 2026 Aditya Singh</span><span>Built around evidence, not noise.</span><a href="/">Back to top ↑</a></footer>
  </div>;
}
