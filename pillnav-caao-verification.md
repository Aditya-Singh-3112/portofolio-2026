# PillNav and CAAO explainer verification

The shared header now uses animated pill navigation with a visible active route, hover fill motion, rotating brand mark, and a responsive mobile menu. The Research page includes a lightweight SVG/CSS CAAO explainer showing model decomposition, CPU RAM and GPU VRAM, PCIe/DMA transfer, compute, offload, update, and the sliding-window VRAM payoff. Desktop and mobile screenshots confirm readable composition and responsive containment. Reduced-motion behavior is implemented, and typecheck plus production build pass.
